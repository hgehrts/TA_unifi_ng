[install]
is_configured = false
state = enabled
build = 1781373080

[launcher]
author = TA UniFi NG
description = Polls UniFi Network Integration API v1 into Splunk: sites, devices, clients, networks, firewall, wifi, wan, vpn, switching, dns, traffic lists, radius, vouchers, device tags, pending devices, info.
version = 3.3.0

[ui]
is_visible = true
label = UniFi Network Add-on for Splunk
supported_themes = light, dark

[package]
check_for_updates = true
id = TA_unifi_ng

[id]
name = TA_unifi_ng
version = 3.3.0

[triggers]
reload.ta_unifi_ng_settings = simple
reload.ta_unifi_ng_account = simple
